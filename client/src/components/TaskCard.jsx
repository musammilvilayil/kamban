import { CalendarDays, Pencil, Trash2 } from "lucide-react";
const getInitials=(name)=>!name||name==="Unassigned"?"?":name.split(" ").map(p=>p[0]).join("").slice(0,2).toUpperCase();
function TaskCard({task,onEdit,onDelete}){const formattedDate=new Intl.DateTimeFormat("en",{month:"short",day:"numeric"}).format(new Date(task.date));return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between gap-3"><h3 className="text-sm font-semibold leading-5 text-slate-900">{task.title}</h3><div className="flex shrink-0 items-center gap-1"><button type="button" aria-label={`Edit ${task.title}`} onClick={()=>onEdit(task)} className="grid h-8 w-8 place-items-center rounded-lg text-slate-400 hover:bg-slate-100 hover:text-slate-700"><Pencil size={15}/></button><button type="button" aria-label={`Delete ${task.title}`} onClick={()=>onDelete(task._id)} className="grid h-8 w-8 place-items-center rounded-lg text-slate-400 hover:bg-rose-50 hover:text-rose-600"><Trash2 size={15}/></button></div></div><div className="mt-5 flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-2"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-slate-100 text-[11px] font-bold text-slate-600">{getInitials(task.assignee)}</div><span className="truncate text-xs font-medium text-slate-600">{task.assignee}</span></div><div className="flex shrink-0 items-center gap-1.5 text-xs text-slate-400"><CalendarDays size={14}/>{formattedDate}</div></div></article>}
export default TaskCard;
