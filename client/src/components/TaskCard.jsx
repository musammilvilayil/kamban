import { CalendarDays, Pencil, Trash2 } from "lucide-react";
const getInitials=(name)=>!name||name==="Unassigned"?"?":name.split(" ").map(p=>p[0]).join("").slice(0,2).toUpperCase();
function TaskCard({task,onEdit,onDelete}){const deadline=task.deadline?new Date(task.deadline):null;const formattedDeadline=deadline&&!Number.isNaN(deadline.getTime())?new Intl.DateTimeFormat("en",{month:"short",day:"numeric"}).format(deadline):null;return <article className="rounded-2xl border border-slate-200 bg-white p-3.5 shadow-sm transition sm:p-4 sm:hover:-translate-y-0.5 sm:hover:shadow-md"><div className="flex items-start justify-between gap-2"><h3 className="min-w-0 break-words pt-1 text-sm font-semibold leading-5 text-slate-900">{task.title}</h3><div className="flex shrink-0 items-center gap-0.5"><button type="button" aria-label={`Edit ${task.title}`} onClick={()=>onEdit(task)} className="grid h-11 w-11 place-items-center rounded-xl text-slate-400 active:bg-slate-100 active:text-slate-700 sm:h-9 sm:w-9 sm:hover:bg-slate-100 sm:hover:text-slate-700"><Pencil size={15}/></button><button type="button" aria-label={`Delete ${task.title}`} onClick={()=>onDelete(task._id)} className="grid h-11 w-11 place-items-center rounded-xl text-slate-400 active:bg-rose-50 active:text-rose-600 sm:h-9 sm:w-9 sm:hover:bg-rose-50 sm:hover:text-rose-600"><Trash2 size={15}/></button></div></div><div className="mt-4 flex items-center justify-between gap-2 sm:mt-5"><div className="flex min-w-0 items-center gap-2"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-slate-100 text-[11px] font-bold text-slate-600">{getInitials(task.assignee)}</div><span className="truncate text-xs font-medium text-slate-600">{task.assignee}</span></div>{formattedDeadline&&<div className="flex shrink-0 items-center gap-1.5 rounded-lg bg-amber-50 px-2 py-1 text-xs font-medium text-amber-700"><CalendarDays size={14}/>Due {formattedDeadline}</div>}</div></article>}
export default TaskCard;
