import { Mic, Send, Sparkles, Square } from "lucide-react";
import { useRef, useState } from "react";
function AIInputBar({onCreateTask,loading}){const[prompt,setPrompt]=useState("");const[listening,setListening]=useState(false);const recognitionRef=useRef(null);const submitPrompt=async(e)=>{e.preventDefault();const value=prompt.trim();if(!value||loading)return;await onCreateTask(value);setPrompt("")};const toggleVoice=()=>{const SpeechRecognition=window.webkitSpeechRecognition;if(!SpeechRecognition){window.alert("Voice input is not supported in this browser. Try Chrome or Edge.");return}if(listening&&recognitionRef.current){recognitionRef.current.stop();return}const recognition=new SpeechRecognition();recognition.continuous=false;recognition.interimResults=false;recognition.lang="en-US";recognition.onstart=()=>setListening(true);recognition.onend=()=>setListening(false);recognition.onerror=()=>setListening(false);recognition.onresult=e=>{const transcript=e.results[0]?.[0]?.transcript||"";setPrompt(current=>`${current} ${transcript}`.trim())};recognitionRef.current=recognition;recognition.start()};return <div className="fixed inset-x-0 bottom-5 z-40 px-4 lg:left-64"><form onSubmit={submitPrompt} className="mx-auto flex max-w-3xl items-center gap-2 rounded-2xl border border-slate-200 bg-white/95 p-2 shadow-2xl shadow-slate-300/50 backdrop-blur"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-100 text-violet-700"><Sparkles size={18}/></div><input value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder='Try: "Create homepage QA task for Maya"' className="h-11 min-w-0 flex-1 bg-transparent px-2 text-sm outline-none placeholder:text-slate-400"/><button type="button" onClick={toggleVoice} aria-label={listening?"Stop listening":"Start voice input"} className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl transition ${listening?"bg-rose-100 text-rose-600":"text-slate-500 hover:bg-slate-100"}`}>{listening?<Square size={16}/>:<Mic size={18}/>}</button><button type="submit" disabled={loading||!prompt.trim()} aria-label="Create task with AI" className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-slate-950 text-white transition hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-40"><Send size={17}/></button></form></div>}
export default AIInputBar;
